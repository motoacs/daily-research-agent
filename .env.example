##
## daily-research-agent (.env example)
## - Secrets must NOT be committed. Use .env locally only.
##

# ---- LLM (OpenRouter; OpenAI-compatible endpoint) ----
# OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Optional: cap completion tokens per request (reduce 402s on large prompts)
# OPENROUTER_MAX_TOKENS=4096
# Optional (recommended by OpenRouter for app attribution)
# OPENROUTER_HTTP_REFERER=
# OPENROUTER_X_TITLE=daily-research-agent

# ---- MCP servers (example; used by Perplexity MCP server) ----
# PERPLEXITY_API_KEY=

# ---- Prompt size controls (optional; reduce prompt tokens) ----
# BOOKMARKS_PROMPT_LIMIT=20
# BOOKMARK_TEXT_MAX_CHARS=500
# BOOKMARK_REFERENCED_MAX=1

# ---- LangSmith (observability / tracing) ----
# LANGSMITH_TRACING=true
# LANGSMITH_API_KEY=
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# LANGSMITH_PROJECT=daily-research-agent
# LANGSMITH_TRACING_SAMPLING_RATE=1.0
# LANGSMITH_WORKSPACE_ID=

# ---- X API v2 (OAuth2 user token with bookmark.read scope) ----
# X_USER_ACCESS_TOKEN=
# X_REFRESH_TOKEN=
X_API_BASE_URL=https://api.x.com
# X_CLIENT_ID=
# X_CLIENT_SECRET=
X_REDIRECT_URI=http://localhost:8000/x/callback
